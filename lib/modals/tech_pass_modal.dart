import 'package:flutter/material.dart';

class TechPassModal extends StatelessWidget {
  const TechPassModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Modal Tech Pass - En desarrollo'),
    );
  }
}
