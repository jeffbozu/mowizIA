import 'package:flutter/material.dart';

class AdminPassModal extends StatelessWidget {
  const AdminPassModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Modal Admin Pass - En desarrollo'),
    );
  }
}
